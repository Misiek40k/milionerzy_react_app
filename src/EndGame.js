import React from 'react';

const EndGame = props => {
    
    
        return (
                <div className='l-end'>Game Over {props.username}</div>
        );
}

export default EndGame;
